# Copy this file to .env.local and fill in real values.
# .env.local is gitignored by default in Vite — never commit real keys.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Flask backend base URL (no trailing slash, no /api suffix)
VITE_API_BASE_URL=http://localhost:5000
